const {nodeWebpackConfig} = require('./node.prop')
const {output} = require('./output.prop')
const {rules} = require('.//rules.prop')

module.exports = {
    nodeWebpackConfig,
    output,
    rules
}